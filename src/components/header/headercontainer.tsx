import Head from 'next/head';

export default function HeaderContainer() {
  return (
    <Head>
      <title>nahs dirt</title>
      <meta name="description" content="nahs dirt" />
      <link rel="icon" href="/favicon.ico" />
    </Head>
  );
}
